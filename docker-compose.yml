version: '3.8'
services:
  wiki:
    image: nginx
    ports:
      - "8080:80"
    networks:
      - frontend
      - backend
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: TitanSecurePassword2026
    networks:
      - backend

networks:
  frontend:
  backend:
    internal: true
